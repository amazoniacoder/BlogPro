# Управление изображениями в Git

## 📦 Что включено в Git

### Статические изображения (всегда в Git):
- ✅ `public/images/placeholder-image.png` - placeholder для отсутствующих изображений
- ✅ `public/placeholder.webp` - основной placeholder
- ✅ `public/uploads/hero/1.webp` - дефолтное hero изображение

### Структура директорий (с .gitkeep):
- ✅ `public/uploads/avatars/.gitkeep`
- ✅ `public/uploads/images/.gitkeep`
- ✅ `public/uploads/thumbnails/.gitkeep`
- ✅ `public/uploads/documents/.gitkeep`
- ✅ `public/uploads/videos/.gitkeep`
- ✅ `public/uploads/audio/.gitkeep`
- ✅ `public/uploads/editor/images/.gitkeep`
- ✅ `public/uploads/editor/temp/.gitkeep`

## ❌ Что НЕ включено в Git

### Пользовательские загрузки (исключены через .gitignore):
- ❌ `public/uploads/avatars/*` - аватары пользователей
- ❌ `public/uploads/images/*` - загруженные изображения
- ❌ `public/uploads/thumbnails/*` - миниатюры
- ❌ `public/uploads/documents/*` - документы
- ❌ `public/uploads/videos/*` - видео
- ❌ `public/uploads/audio/*` - аудио
- ❌ `public/uploads/editor/images/*` - изображения из редактора
- ❌ `public/uploads/editor/temp/*` - временные файлы

**Причина**: Это пользовательские данные, которые должны храниться на сервере и в backup.

---

## 🚀 Деплой на сервер

### Вариант 1: Git (рекомендуется для кода)

```bash
# На локальной машине
git add .
git commit -m "Add static images and directory structure"
git push origin main

# На сервере
cd /root/BlogPro
git pull origin main
```

**Результат**: 
- ✅ Структура директорий создана автоматически
- ✅ Статические изображения загружены
- ❌ Пользовательские загрузки НЕ перезаписаны (сохранены на сервере)

---

### Вариант 2: Rsync (для пользовательских файлов)

Если нужно перенести существующие пользовательские загрузки:

```bash
# С локальной машины на сервер
rsync -avz --progress public/uploads/ root@89.169.0.223:/root/BlogPro/public/uploads/

# Или только определенные директории
rsync -avz --progress public/uploads/avatars/ root@89.169.0.223:/root/BlogPro/public/uploads/avatars/
rsync -avz --progress public/uploads/images/ root@89.169.0.223:/root/BlogPro/public/uploads/images/
```

---

### Вариант 3: SCP (альтернатива rsync)

```bash
# Копирование всей директории uploads
scp -r public/uploads/* root@89.169.0.223:/root/BlogPro/public/uploads/

# Или отдельные файлы
scp public/placeholder.webp root@89.169.0.223:/root/BlogPro/public/
```

---

## 📋 Проверка на сервере

После деплоя проверьте структуру:

```bash
# На сервере
cd /root/BlogPro/public

# Проверить структуру
ls -la uploads/
ls -la uploads/avatars/
ls -la uploads/images/
ls -la images/

# Проверить права доступа
chmod -R 755 uploads/
chown -R www-data:www-data uploads/  # Если используется Nginx/Apache
```

---

## 🔄 Backup пользовательских загрузок

### Создание backup:

```bash
# На сервере
cd /root/BlogPro
tar -czf uploads_backup_$(date +%Y%m%d).tar.gz public/uploads/

# Скачать backup на локальную машину
scp root@89.169.0.223:/root/BlogPro/uploads_backup_*.tar.gz ./backups/
```

### Восстановление из backup:

```bash
# На сервере
cd /root/BlogPro
tar -xzf uploads_backup_20250117.tar.gz
```

---

## 📊 Размер файлов

### Проверка размера uploads:

```bash
# На сервере
du -sh public/uploads/
du -sh public/uploads/*/
```

### Очистка старых файлов:

```bash
# Удалить файлы старше 90 дней (осторожно!)
find public/uploads/editor/temp/ -type f -mtime +90 -delete

# Удалить пустые директории
find public/uploads/ -type d -empty -delete
```

---

## ⚠️ Важные замечания

1. **Git не для больших файлов**: 
   - Git плохо работает с большими бинарными файлами
   - Для больших медиа используйте S3, CDN или отдельное хранилище

2. **Пользовательские данные**:
   - НЕ коммитьте пользовательские загрузки в Git
   - Используйте rsync/scp для переноса данных
   - Настройте регулярные backup

3. **Права доступа**:
   - Убедитесь что веб-сервер может писать в `public/uploads/`
   - Обычно нужны права `755` для директорий и `644` для файлов

4. **Безопасность**:
   - Проверяйте типы загружаемых файлов
   - Ограничивайте размер файлов
   - Используйте WebP для оптимизации

---

## ✅ Чеклист деплоя

- [ ] Закоммитить статические изображения в Git
- [ ] Закоммитить .gitkeep файлы для структуры
- [ ] Push в GitHub
- [ ] Pull на сервере
- [ ] Проверить структуру директорий
- [ ] Установить правильные права доступа
- [ ] (Опционально) Перенести пользовательские файлы через rsync
- [ ] Создать backup существующих uploads
- [ ] Проверить работу загрузки файлов через админку
